<UserControl x:Class="ControlCenter.UserControls.PostProcessingControls.JobPlayStopControlTiny"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlCenter.UserControls.PostProcessingControls"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:circProgress="clr-namespace:WExtraControlLibrary.UserControls.CircularProgressBar;assembly=WExtraControlLibrary"
             xmlns:conv="clr-namespace:ControlCenter.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!--<Button Grid.Column="0" Style="{StaticResource MaterialDesignFloatingActionLightButton}"  VerticalAlignment="Center" HorizontalAlignment="Center"
                Content="{materialDesign:PackIcon Stop}"
                Margin="10"
                Command="{Binding StopCommand}"
                Width="32" Height="32"
                CommandParameter="{Binding ElementName=DragablzItemsControl}"/>-->
        <Button Grid.Column="2" Style="{StaticResource MaterialDesignFloatingActionLightButton}"  VerticalAlignment="Center" HorizontalAlignment="Center"
                Content="{Binding Phase, Converter={conv:Phase2IconKind}}"
                Margin="10"
                Command="{Binding StartCommand}"
                Width="38" Height="38"
                CommandParameter="{Binding ElementName=DragablzItemsControl}"/>
        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.RowSpan="2" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--<TextBlock Text="{Binding Phase}"  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5" FontSize="12"/>-->
            <TextBlock IsEnabled="False" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0" FontSize="10">
                <Run Text="{Binding Percentage}"/>
                <Run Text=" %"/>
            </TextBlock>
        </StackPanel>
        <ProgressBar Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Value="{Binding Percentage}" Minimum="0" Maximum="100" materialDesign:TransitionAssist.DisableTransitions="True" Margin="0,0,0,2"></ProgressBar>
    </Grid>
</UserControl>
