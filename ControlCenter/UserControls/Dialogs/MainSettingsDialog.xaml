<UserControl x:Class="ControlCenter.UserControls.Dialogs.MainSettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlCenter.UserControls.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:properties="clr-namespace:ControlCenter.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="TB_Title" TextWrapping="Wrap" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,35,20,50" FontSize="16" TextDecorations="Underline">Drobit Settings</TextBlock>
        <TextBlock x:Name="TB_Folder" Text="{Binding Source={x:Static properties:Settings.Default}, Path=DrobitMainPath}" Grid.Row="1" Margin="10"/>
        <Button Content="{materialDesign:PackIcon Kind=Folder}" Style="{DynamicResource MaterialDesignFlatButton}" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0" HorizontalAlignment="Center" Click="Button_Click"/>
        <Button x:Name="B_SAVE" IsEnabled="False" IsDefault="True" Grid.Row="2" Grid.Column="0" MinWidth="100" Height="30" Style="{DynamicResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                <Button.CommandParameter>
                    <system:Boolean>True</system:Boolean>
                </Button.CommandParameter>
                Save
            </Button>
        <Button IsCancel="True" Grid.Column="1" Grid.Row="2" MinWidth="100" Height="30" Margin="8 0 0 0" Style="{DynamicResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
            <Button.CommandParameter>
                <system:Boolean>False</system:Boolean>
            </Button.CommandParameter>
            Cancel
        </Button>
        
    </Grid>
</UserControl>
