<UserControl x:Class="ControlCenter.UserControls.Dialogs.AutoUpdaterDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlCenter.UserControls.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:properties="clr-namespace:ControlCenter.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="TB_Title" TextWrapping="Wrap" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,35,20,5" FontSize="16" TextDecorations="Underline">New Software version found!</TextBlock>
        <TextBlock x:Name="TB_Version" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,5,0,30"/>
        <TextBlock x:Name="TB_Changelog" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Margin="10,10,10,20"/>
        <Button x:Name="B_Update" IsDefault="True" Grid.Row="3" Grid.Column="0" MinWidth="100" Height="30" Style="{DynamicResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                <Button.CommandParameter>
                    <system:Boolean>True</system:Boolean>
                </Button.CommandParameter>
                Update
            </Button>
        <Button IsCancel="True" Grid.Column="1" Grid.Row="3" MinWidth="100" Height="30" Margin="8 0 0 0" Style="{DynamicResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
            <Button.CommandParameter>
                <system:Boolean>False</system:Boolean>
            </Button.CommandParameter>
            Later
        </Button>
    </Grid>
</UserControl>
